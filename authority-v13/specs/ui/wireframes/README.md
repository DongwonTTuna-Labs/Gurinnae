# Final Structural Wireframes

이 디렉터리의 wireframe은 시각적 초안이 아니라 각 화면군의 구조적 보조 설명이다.
정확한 색상·typography·spacing·grid는 `FINAL_DESIGN_SYSTEM.md`와
`design-tokens.yaml`, 정확한 화면 순서는 `screen-build-manifest.yaml`이 권위다.

구현자는 wireframe을 이유로 정보 순서를 바꾸거나 API·상태·권한을 생략할 수 없다.
