# CAS-002 — 사건 Overview

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/internal/cases/{caseId}/overview`  
Surface: `internal`  
Access: `oidc`  
Archetype: `WORKSPACE`  
Test prefix: `cas_002`

## 사용자 목적

사건의 조사 질문·known/unknown/response·blocker·다음 작업을 한눈에 파악한다.

## 이 화면이 즉시 답해야 하는 질문

- 이 사건은 무엇을 조사하는가?
- 현재 확인·미확인·소명은 무엇인가?
- 다음 필수 작업은 무엇인가?

## Above-the-fold 순서

1. `header`
2. `known-unknown`
3. `readiness`
4. `next`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `header` | identity·state·version | `StructuredContentSection` | assignment·due. |
| 2 | `known-unknown` | Known/Unknown/Response | `KnownUnknownResponse` | 핵심 요약. |
| 3 | `readiness` | 진행과 gate | `StructuredContentSection` | blocking task. |
| 4 | `next` | 다음 작업 | `AccessManagementPanel` | role-aware. |
| 5 | `activity` | 최근 변경 | `StructuredContentSection` | audit-linked. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: internal shell: 248px nav + 232px task rail + fluid task content + optional 360px context
- Medium: nav collapses; task rail becomes horizontal tabs; context becomes drawer
- Compact: single task at a time; blockers before task body; persistent save state
- Container: `workspace`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `start-next` | 다음 작업 수행 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `transition` | 상태 전이 | `COMMAND` | `ACTIVE_SESSION` | `cases.investigate` | `transitionCase` |
| `assign` | 담당자 변경 | `COMMAND` | `ACTIVE_SESSION` | `cases.assign` | `assignCase` |

Primary action: `transition`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `getInternalCase` | `control-api` | `GET` | `/v1/internal/queries/get-internal-case` | `getInternalCaseQuery` | `InternalCaseResponse` |
| `getCaseWorkspaceOverview` | `control-api` | `GET` | `/v1/internal/queries/get-case-workspace-overview` | `getCaseWorkspaceOverviewQuery` | `CaseWorkspaceOverviewResponse` |
| `transitionCase` | `control-api` | `POST` | `/v1/internal/commands/transition-case` | `transitionCaseRequest` | `transitionCaseReceipt` |
| `assignCase` | `control-api` | `POST` | `/v1/internal/commands/assign-case` | `assignCaseRequest` | `assignCaseReceipt` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`
- `unauthorized`
- `forbidden`
- `conflict`
- `reauth-required`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `internal.case_overview.viewed`
- `internal.case.next_task_opened`
- `internal.case.transitioned`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- mutable aggregate 직접 PATCH
- blocker 숨김

## Implementation files

- Page: `apps/review-console/src/routes/internal/cases/[caseId]/overview/+page.svelte`
- Server load/actions: `apps/review-console/src/routes/internal/cases/[caseId]/overview/+page.server.ts`
- Layout: `apps/review-console/src/routes/internal/+layout.server.ts`
- Error boundary: `apps/review-console/src/routes/+error.svelte`
- View model: `apps/review-console/src/lib/view-models/cas-002.ts`
- E2E: `tests/e2e/screens/cas-002.spec.ts`
- Visual: `tests/visual/cas-002.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
