schema_version: 4
specification_version: 13.0.0
status: FINAL
environment: isolated PostgreSQL 18.4 after all migrations
execution: verification/postgres-runtime/verify.mjs writes dynamic output to an external temporary file; comparator checks
  the stable baseline and authority-tree digest before/after must match
test_count: 58
expanded_runtime_assertion_count: 133
tests:
- id: DBSEC-001
  role: migration test role
  setup: empty PostgreSQL 18.4 database
  action: apply all ordered migrations
  expected: all-23-migrations-pass
- id: DBSEC-002
  role: migration test role
  setup: migrated database
  action: inspect pg_extension namespaces
  expected: pgcrypto-citext-pg_trgm-in-extensions
- id: DBSEC-003
  role: migration test role
  setup: migrated database
  action: inspect pg_proc proconfig for SECURITY DEFINER functions
  expected: trusted-search-path-ending-pg_temp
- id: DBSEC-004
  role: migration test role
  setup: migrated database
  action: resolve append_audit_event regprocedure and identity grant
  expected: exact-signature-and-execute-grant
- id: DBSEC-005
  role: gurine_control_api
  setup: empty audit stream
  action: append two audit events
  expected: linked-hashes-and-head-version-two
- id: DBSEC-006
  role: gurine_public_api
  setup: migrated database
  action: SELECT editorial.cases
  expected: permission-denied
- id: DBSEC-007
  role: gurine_public_api
  setup: migrated database
  action: INSERT public.cases
  expected: permission-denied
- id: DBSEC-008
  role: gurine_identity_api
  setup: migrated database
  action: SELECT editorial.cases
  expected: permission-denied
- id: DBSEC-009
  role: gurine_submission_api
  setup: migrated database
  action: direct INSERT intake.response_submissions
  expected: permission-denied
- id: DBSEC-010
  role: all runtime privilege roles
  setup: new ungranted ops table
  action: SELECT ungranted table
  expected: permission-denied-for-every-role
- id: DBSEC-011
  role: gurine_submission_api
  setup: verified open request without draft
  action: save draft with expectedVersion=0
  expected: create-version-one
- id: DBSEC-012
  role: gurine_submission_api
  setup: verified open request without draft
  action: save draft with expectedVersion>0
  expected: version-conflict
- id: DBSEC-013
  role: gurine_submission_api
  setup: version-one clean draft
  action: submit response
  expected: one-submission-request-submitted-token-revoked
- id: DBSEC-014
  role: gurine_submission_api
  setup: already submitted request
  action: submit again
  expected: closed-or-consumed-token-error
- id: DBSEC-015
  role: migration test role
  setup: existing submission
  action: direct second insert for request
  expected: unique-response-request-constraint
- id: DBSEC-016
  role: gurine_submission_api
  setup: closed response request
  action: save response draft
  expected: closed-request-error
- id: DBSEC-017
  role: gurine_submission_api
  setup: expired response request
  action: save response draft
  expected: expired-request-error
- id: DBSEC-018
  role: gurine_submission_api
  setup: malicious caller search_path
  action: call token-scoped function
  expected: fixed-function-search-path
- id: DBSEC-019
  role: gurine_identity_api
  setup: active session with current CSRF hash
  action: rotate session CSRF hash
  expected: true-and-new-hash-persisted
- id: DBSEC-020
  role: gurine_identity_api
  setup: session after CSRF rotation
  action: rotate with stale current hash
  expected: false-and-hash-unchanged
- id: DBSEC-021
  role: migration test role
  setup: migrated database
  action: inspect runtime privilege roles
  expected: no-superuser-no-bypassrls
- id: DBSEC-022
  role: gurine_control_api
  setup: existing audit event
  action: UPDATE audit event
  expected: permission-or-immutable-trigger-denial
- id: DBSEC-023
  role: gurine_ingest_worker
  setup: FETCHED source document
  action: transition to PARSED with parser metadata
  expected: valid-lifecycle-transition
- id: DBSEC-024
  role: gurine_ingest_worker
  setup: PARSED source document
  action: transition backward to FETCHED
  expected: invalid-transition-denied
- id: DBSEC-025
  role: gurine_analysis_worker
  setup: RUNNING rule run
  action: transition to SUCCEEDED with terminal metrics
  expected: valid-terminal-transition
- id: DBSEC-026
  role: gurine_analysis_worker
  setup: SUCCEEDED rule run
  action: mutate terminal metrics
  expected: terminal-mutation-denied
- id: DBSEC-027
  role: gurine_control_api
  setup: immutable review snapshot
  action: UPDATE snapshot payload
  expected: permission-or-immutable-trigger-denial
- id: DBSEC-028
  role: gurine_control_api
  setup: fresh actor assertion JTI
  action: consume twice through ops.consume_assertion_jti
  expected: first-true-second-false
- id: DBSEC-029
  role: migration test role
  setup: migrated database
  action: inspect active pg_catalog objects
  expected: 105-tables-40-functions-36-triggers-5-policies
- id: DBSEC-030
  role: migration test role
  setup: migrated database and optimistic concurrency runtime projection
  action: resolve every guard/mutation relation, column and request/context placeholder
  expected: all-62-concurrency-contracts-resolve
- id: DBSEC-031
  role: gurine_control_api
  setup: OPEN schema drift at version one and exact mapping proposal
  action: approve mapping with expectedVersion=1 then retry stale version
  expected: first-update-one-row-stale-update-zero-rows
- id: DBSEC-032
  role: gurine_control_api
  setup: OPEN schema drift at version one and exact mapping proposal
  action: reject exact mapping by drift/version/digest then retry stale version
  expected: selected-mapping-rejected-drift-version-incremented-stale-rejected
- id: DBSEC-033
  role: gurine_control_api
  setup: queue control and source registry at version one
  action: pause using queue_name/source_id keys and retry stale versions
  expected: correct-primary-key-predicates-and-stale-rejection
- id: DBSEC-034
  role: gurine_identity_api
  setup: fresh service assertion JTI
  action: consume twice
  expected: first-true-second-false
- id: DBSEC-035
  role: gurine_control_api
  setup: fresh actor assertion JTI
  action: consume twice
  expected: first-true-second-false
- id: DBSEC-036
  role: gurine_identity_api
  setup: active session and open step-up authorization
  action: claim the exact authorization three times
  expected: three-fresh-actor-assertion-issue-slots
- id: DBSEC-037
  role: gurine_identity_api
  setup: authorization with three issued assertions
  action: claim a fourth time
  expected: invalid-or-exhausted-authorization-error
- id: DBSEC-038
  role: gurine_identity_api
  setup: open authorization
  action: close exact authorization
  expected: closed-and-future-claims-rejected
- id: DBSEC-039
  role: gurine_control_api
  setup: open authorization
  action: call claim_step_up_authorization
  expected: permission-denied
- id: DBSEC-040
  role: migration test role
  setup: all migrations applied
  action: resolve ops.step_up_proofs and ops.consume_step_up_proof
  expected: both-legacy-objects-absent
- id: DBSEC-041
  role: gurine_control_api / gurine_workflow_worker / denied roles
  setup: queued audit export request
  action: create, read and terminalize export through declared roles
  expected: control-create-workflow-update-public-submission-auditor-write-denied
- id: DBSEC-042
  role: gurine_control_api
  setup: case and immutable review snapshot at case version one
  action: place exact scoped legal hold then retry stale case version
  expected: one-hold-case-version-two-stale-update-zero-rows
- id: DBSEC-043
  role: gurine_control_api
  setup: placed active legal hold
  action: attempt UPDATE and inspect UPDATE/DELETE privileges
  expected: mutation-permission-denied-and-no-update-delete-privilege
- id: DBSEC-044
  role: gurine_submission_api
  setup: fresh BFF service assertion JTI
  action: consume the exact JTI twice
  expected: first-true-second-false
- id: DBSEC-045
  role: gurine_submission_api
  setup: fresh response magic token
  action: exchange token for a pending scoped session
  expected: pending-response-session-issued-and-token-consumed
- id: DBSEC-046
  role: gurine_submission_api
  setup: already consumed response magic token
  action: exchange the same token again
  expected: replay-rejected
- id: DBSEC-047
  role: gurine_submission_api
  setup: pending response session and correct email OTP
  action: promote session
  expected: active-response-session-issued-and-pending-session-consumed
- id: DBSEC-048
  role: gurine_submission_api
  setup: consumed pending response session
  action: promote or reuse again
  expected: reuse-rejected
- id: DBSEC-049
  role: gurine_submission_api
  setup: active response session
  action: save scoped draft and create finalized clean attachment
  expected: same-scope-write-succeeds
- id: DBSEC-050
  role: gurine_submission_api
  setup: two active response sessions for different requests
  action: access another session attachment
  expected: cross-session-idor-rejected
- id: DBSEC-051
  role: gurine_submission_api
  setup: active response session with final clean attachment
  action: submit response and resolve receipt
  expected: single-submit-receipt-and-session-revocation
- id: DBSEC-052
  role: gurine_submission_api
  setup: revoked response session
  action: save draft or submit again
  expected: reuse-rejected
- id: DBSEC-053
  role: gurine_submission_api
  setup: active correction draft session
  action: save, attach, preview and atomically submit
  expected: immutable-correction-request-receipt-and-session-revocation
- id: DBSEC-054
  role: gurine_submission_api
  setup: two correction sessions
  action: access another correction attachment
  expected: cross-session-idor-rejected
- id: DBSEC-055
  role: gurine_submission_api
  setup: submitted correction session
  action: reuse draft session
  expected: reuse-rejected
- id: DBSEC-056
  role: gurine_submission_api
  setup: subscription verification and management sessions
  action: verify, manage and unsubscribe
  expected: scoped-lifecycle-succeeds-and-session-revoked
- id: DBSEC-057
  role: gurine_submission_api
  setup: revoked subscription session
  action: manage subscription again
  expected: reuse-rejected
- id: DBSEC-058
  role: gurine_submission_api
  setup: all v13 scoped-session relations
  action: attempt direct table mutation
  expected: least-privilege-permission-denied
