schema_version: 1
specification_version: 13.0.0
status: FINAL
contract_count: 65
contracts:
- operation_id: acceptAgentSuggestion
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.agent_suggestions
  guard_identity_predicate: id = :suggestionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.agent_suggestions
  mutation_identity_predicate: id = :suggestionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - suggestionId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: acknowledgeSourceIncident
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.source_incidents
  guard_identity_predicate: source_id = :sourceId AND status = 'OPEN'
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.source_incidents
  mutation_identity_predicate: source_id = :sourceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - sourceId
  context_placeholders: []
  guard_columns:
  - source_id
  - status
  - version
  mutation_columns:
  - source_id
  - version
- operation_id: activateKillSwitch
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.kill_switches
  guard_identity_predicate: id = :killSwitchId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.kill_switches
  mutation_identity_predicate: id = :killSwitchId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - killSwitchId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: activateRuleVersion
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.rule_versions
  guard_identity_predicate: id = :ruleVersionId
  guard_version_predicate: row_version = :expectedVersion
  mutation_relation: core.rule_versions
  mutation_identity_predicate: id = :ruleVersionId
  success_effect: increment row_version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: row_version = row_version + 1
  request_placeholders:
  - expectedVersion
  - ruleVersionId
  context_placeholders: []
  guard_columns:
  - id
  - row_version
  mutation_columns:
  - id
  - row_version
- operation_id: addClaim
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: addEvidence
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: approveResponseExcerpt
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.responses
  guard_identity_predicate: id = :responseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.responses
  mutation_identity_predicate: id = :responseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - responseId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: approveSchemaMapping
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.schema_drifts
  guard_identity_predicate: id = :schemaDriftId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.schema_drifts
  mutation_identity_predicate: id = :schemaDriftId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - schemaDriftId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: assignCase
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: assignCorrection
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.corrections
  guard_identity_predicate: id = :correctionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.corrections
  mutation_identity_predicate: id = :correctionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - correctionId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: assignReview
  mode: OPTIMISTIC
  version_field: expectedCaseVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedCaseVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedCaseVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: assignSignal
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.anomaly_signals
  guard_identity_predicate: id = :signalId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: core.anomaly_signals
  mutation_identity_predicate: id = :signalId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - signalId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: cancelJob
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.jobs
  guard_identity_predicate: id = :jobId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.jobs
  mutation_identity_predicate: id = :jobId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - jobId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createCorrection
  mode: OPTIMISTIC
  version_field: expectedCaseVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  guard_version_predicate: version = :expectedCaseVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedCaseVersion
  - publicationId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createCorrectionRequest
  mode: OPTIMISTIC
  version_field: expectedVersion
  execution:
    kind: SECURITY_DEFINER_FUNCTION
    function: intake.submit_correction_draft_session
    signature: char(64),text,bigint,boolean,boolean,char(64),char(64),timestamptz
    caller: gurine_submission_api
  guard_relation: intake.correction_request_drafts
  guard_identity_predicate: id = :sessionScopeId
  guard_version_predicate: version = :expectedVersion
  guard_lock: SELECT FOR UPDATE inside intake.submit_correction_draft_session
  mutation_relation: intake.correction_requests
  mutation_identity_predicate: id = generated
  version_effect: GUARD_ONLY_TERMINALIZE
  version_mutation: guarded draft is terminalized exactly once
  success_effect: scoped session resolves exactly one aggregate and the exact expected version is enforced
  zero_rows_resolution:
    probe: read scoped aggregate identity without version predicate inside the same fixed-search-path function
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  request_placeholders:
  - expectedVersion
  context_placeholders:
  - sessionScopeId
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  terminal_requirements:
  - verb: INSERT
    relation: intake.correction_requests
  - verb: UPDATE
    relation: intake.submission_sessions
    state_contains: revoked_at
  - verb: DELETE
    relation: intake.correction_request_drafts
- operation_id: createEvidenceRedaction
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.evidence
  guard_identity_predicate: id = :evidenceId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.evidence
  mutation_identity_predicate: id = :evidenceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - evidenceId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createHypothesis
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createResponseRequest
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createRetractionDraft
  mode: OPTIMISTIC
  version_field: expectedCaseVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  guard_version_predicate: version = :expectedCaseVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedCaseVersion
  - publicationId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: createReviewSnapshot
  mode: OPTIMISTIC
  version_field: expectedCaseVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedCaseVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: SNAPSHOT_STALE
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedCaseVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: deactivateKillSwitch
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.kill_switches
  guard_identity_predicate: id = :killSwitchId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.kill_switches
  mutation_identity_predicate: id = :killSwitchId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - killSwitchId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: deleteCorrectionRequestDraft
  mode: OPTIMISTIC
  version_field: expectedVersion
  execution:
    kind: SECURITY_DEFINER_FUNCTION
    function: intake.delete_correction_draft_session
    signature: char(64),text,bigint
    caller: gurine_submission_api
  guard_relation: intake.correction_request_drafts
  guard_identity_predicate: id = :sessionScopeId
  guard_version_predicate: version = :expectedVersion
  guard_lock: SELECT FOR UPDATE inside intake.delete_correction_draft_session
  mutation_relation: intake.correction_request_drafts
  mutation_identity_predicate: id = :sessionScopeId
  version_effect: GUARD_ONLY_TERMINALIZE
  version_mutation: guarded draft is terminalized exactly once
  success_effect: scoped session resolves exactly one aggregate and the exact expected version is enforced
  zero_rows_resolution:
    probe: read scoped aggregate identity without version predicate inside the same fixed-search-path function
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  request_placeholders:
  - expectedVersion
  context_placeholders:
  - sessionScopeId
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  terminal_requirements:
  - verb: DELETE
    relation: intake.correction_request_drafts
  - verb: UPDATE
    relation: intake.submission_sessions
    state_contains: revoked_at
- operation_id: deleteSavedView
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.saved_views
  guard_identity_predicate: id = :savedViewId AND user_id = :actorUserId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.saved_views
  mutation_identity_predicate: id = :savedViewId AND user_id = :actorUserId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - savedViewId
  context_placeholders:
  - actorUserId
  guard_columns:
  - id
  - user_id
  - version
  mutation_columns:
  - id
  - user_id
  - version
- operation_id: disableProviderRouting
  mode: AUTHORIZED_EXECUTION_FENCE
  version_field: expectedVersion
  guard_relation: ops.provider_configs
  guard_identity_predicate: id = :providerId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.provider_configs
  mutation_identity_predicate: id = :providerId
  success_effect: validate and mutate only inside private.ExecuteProviderControl after a current ExecutionAuthorization; direct
    HTTP performs no mutation
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - providerId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
  direct_http_effect: NONE
  execution_owner: private.ExecuteProviderControl
- operation_id: disableUser
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.users
  guard_identity_predicate: id = :userId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.users
  mutation_identity_predicate: id = :userId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - userId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: extendKillSwitch
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.kill_switches
  guard_identity_predicate: id = :killSwitchId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.kill_switches
  mutation_identity_predicate: id = :killSwitchId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - killSwitchId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: grantRole
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.users
  guard_identity_predicate: id = :userId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.users
  mutation_identity_predicate: id = :userId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - userId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: linkEvidence
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.evidence
  guard_identity_predicate: id = :evidenceId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.evidence
  mutation_identity_predicate: id = :evidenceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - evidenceId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: linkSignalToCase
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.anomaly_signals
  guard_identity_predicate: id = :signalId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: core.anomaly_signals
  mutation_identity_predicate: id = :signalId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - signalId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: markNotificationRead
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.notifications
  guard_identity_predicate: id = :notificationId AND user_id = :actorUserId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.notifications
  mutation_identity_predicate: id = :notificationId AND user_id = :actorUserId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - notificationId
  context_placeholders:
  - actorUserId
  guard_columns:
  - id
  - user_id
  - version
  mutation_columns:
  - id
  - user_id
  - version
- operation_id: pauseBackfill
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.source_runs
  guard_identity_predicate: id = :backfillRunId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.source_runs
  mutation_identity_predicate: id = :backfillRunId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - backfillRunId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: pauseJobQueue
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.queue_controls
  guard_identity_predicate: queue_name = :queueName
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.queue_controls
  mutation_identity_predicate: queue_name = :queueName
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - queueName
  context_placeholders: []
  guard_columns:
  - queue_name
  - version
  mutation_columns:
  - queue_name
  - version
- operation_id: pauseSource
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.source_registry
  guard_identity_predicate: source_id = :sourceId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.source_registry
  mutation_identity_predicate: source_id = :sourceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - sourceId
  context_placeholders: []
  guard_columns:
  - source_id
  - version
  mutation_columns:
  - source_id
  - version
- operation_id: placeTemporaryRestriction
  mode: OPTIMISTIC
  version_field: expectedCaseVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  guard_version_predicate: version = :expectedCaseVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = (SELECT case_id FROM editorial.publication_revisions WHERE id = :publicationId)
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedCaseVersion
  - publicationId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: proposeRoleDefinitionChange
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.roles
  guard_identity_predicate: id = :roleId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.roles
  mutation_identity_predicate: id = :roleId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - roleId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: publishCase
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: quarantineJob
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.jobs
  guard_identity_predicate: id = :jobId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.jobs
  mutation_identity_predicate: id = :jobId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - jobId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: reassignTask
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.tasks
  guard_identity_predicate: id = :taskId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.tasks
  mutation_identity_predicate: id = :taskId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - taskId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: rejectAgentSuggestion
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.agent_suggestions
  guard_identity_predicate: id = :suggestionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.agent_suggestions
  mutation_identity_predicate: id = :suggestionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - suggestionId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: rejectSchemaMapping
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.schema_drifts
  guard_identity_predicate: id = :schemaDriftId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.schema_drifts
  mutation_identity_predicate: id = :schemaDriftId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - schemaDriftId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: resolveCorrectionRequest
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.corrections
  guard_identity_predicate: id = :correctionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.corrections
  mutation_identity_predicate: id = :correctionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - correctionId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: retryJob
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.jobs
  guard_identity_predicate: id = :jobId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.jobs
  mutation_identity_predicate: id = :jobId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - jobId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: retrySourceRun
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.source_runs
  guard_identity_predicate: id = :sourceRunId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.source_runs
  mutation_identity_predicate: id = :sourceRunId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - sourceRunId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: revokeRole
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.users
  guard_identity_predicate: id = :userId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.users
  mutation_identity_predicate: id = :userId
  success_effect: increment version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - userId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: rollbackRuleVersion
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.rule_versions
  guard_identity_predicate: id = :ruleVersionId
  guard_version_predicate: row_version = :expectedVersion
  mutation_relation: core.rule_versions
  mutation_identity_predicate: id = :ruleVersionId
  success_effect: increment row_version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: row_version = row_version + 1
  request_placeholders:
  - expectedVersion
  - ruleVersionId
  context_placeholders: []
  guard_columns:
  - id
  - row_version
  mutation_columns:
  - id
  - row_version
- operation_id: saveCorrectionRequestDraft
  mode: OPTIMISTIC
  version_field: expectedVersion
  execution:
    kind: SECURITY_DEFINER_FUNCTION
    function: intake.save_correction_draft_session
    signature: char(64),text,bigint,text,char(64),bytea,text,jsonb,text
    caller: gurine_submission_api
  guard_relation: intake.correction_request_drafts
  guard_identity_predicate: id = :sessionScopeId
  guard_version_predicate: version = :expectedVersion
  guard_lock: SELECT FOR UPDATE inside intake.save_correction_draft_session
  mutation_relation: intake.correction_request_drafts
  mutation_identity_predicate: id = :sessionScopeId
  version_effect: INCREMENT_ONE
  version_mutation: version = version + 1
  success_effect: scoped session resolves exactly one aggregate and the exact expected version is enforced
  zero_rows_resolution:
    probe: read scoped aggregate identity without version predicate inside the same fixed-search-path function
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  request_placeholders:
  - expectedVersion
  context_placeholders:
  - sessionScopeId
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: saveResponseDraft
  mode: OPTIMISTIC
  version_field: expectedVersion
  execution:
    kind: SECURITY_DEFINER_FUNCTION
    function: intake.save_response_draft_session
    signature: char(64),text,bigint,bytea,jsonb,timestamptz
    caller: gurine_submission_api
  guard_relation: intake.response_drafts
  guard_identity_predicate: response_request_id = :sessionScopeId
  guard_version_predicate: version = :expectedVersion
  guard_lock: SELECT FOR UPDATE inside intake.save_response_draft_session
  mutation_relation: intake.response_drafts
  mutation_identity_predicate: response_request_id = :sessionScopeId
  version_effect: CREATE_AT_ONE_OR_INCREMENT_ONE
  version_mutation: new row version = 1; existing row version = version + 1
  success_effect: scoped session resolves exactly one aggregate and the exact expected version is enforced
  zero_rows_resolution:
    probe: read scoped aggregate identity without version predicate inside the same fixed-search-path function
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  request_placeholders:
  - expectedVersion
  context_placeholders:
  - sessionScopeId
  guard_columns:
  - response_request_id
  - version
  mutation_columns:
  - response_request_id
  - version
  creation_predicate: no draft exists AND :expectedVersion = 0
- operation_id: saveResponseRequestDraft
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.response_requests
  guard_identity_predicate: id = :responseRequestId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.response_requests
  mutation_identity_predicate: id = :responseRequestId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - responseRequestId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: scheduleRuleActivation
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.rule_versions
  guard_identity_predicate: id = :ruleVersionId
  guard_version_predicate: row_version = :expectedVersion
  mutation_relation: core.rule_versions
  mutation_identity_predicate: id = :ruleVersionId
  success_effect: increment row_version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: row_version = row_version + 1
  request_placeholders:
  - expectedVersion
  - ruleVersionId
  context_placeholders: []
  guard_columns:
  - id
  - row_version
  mutation_columns:
  - id
  - row_version
- operation_id: setModelAutoUpgrade
  mode: AUTHORIZED_EXECUTION_FENCE
  version_field: expectedVersion
  guard_relation: ops.provider_configs
  guard_identity_predicate: id = :providerId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.provider_configs
  mutation_identity_predicate: id = :providerId
  success_effect: validate and mutate only inside private.ExecuteProviderControl after a current ExecutionAuthorization; direct
    HTTP performs no mutation
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - providerId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - routing_policy
  - version
  direct_http_effect: NONE
  execution_owner: private.ExecuteProviderControl
- operation_id: startRuleShadow
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.rule_versions
  guard_identity_predicate: id = :ruleVersionId
  guard_version_predicate: row_version = :expectedVersion
  mutation_relation: core.rule_versions
  mutation_identity_predicate: id = :ruleVersionId
  success_effect: increment row_version by exactly one
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: row_version = row_version + 1
  request_placeholders:
  - expectedVersion
  - ruleVersionId
  context_placeholders: []
  guard_columns:
  - id
  - row_version
  mutation_columns:
  - id
  - row_version
- operation_id: submitResponse
  mode: OPTIMISTIC
  version_field: expectedVersion
  execution:
    kind: SECURITY_DEFINER_FUNCTION
    function: intake.submit_response_session
    signature: char(64),text,bigint,char(64),bytea,jsonb,char(64),char(64),timestamptz
    caller: gurine_submission_api
  guard_relation: intake.response_drafts
  guard_identity_predicate: response_request_id = :sessionScopeId
  guard_version_predicate: version = :expectedVersion
  guard_lock: SELECT FOR UPDATE inside intake.submit_response_session
  mutation_relation: intake.response_submissions
  mutation_identity_predicate: response_request_id = :sessionScopeId
  version_effect: GUARD_ONLY_TERMINALIZE
  version_mutation: guarded draft is terminalized exactly once
  success_effect: scoped session resolves exactly one aggregate and the exact expected version is enforced
  zero_rows_resolution:
    probe: read scoped aggregate identity without version predicate inside the same fixed-search-path function
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  request_placeholders:
  - expectedVersion
  context_placeholders:
  - sessionScopeId
  guard_columns:
  - response_request_id
  - version
  mutation_columns:
  - response_request_id
  terminal_requirements:
  - verb: INSERT
    relation: intake.response_submissions
  - verb: UPDATE
    relation: editorial.response_requests
    version_mutation: version = version + 1
  - verb: UPDATE
    relation: intake.submission_sessions
    state_contains: revoked_at
- operation_id: transitionCase
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.cases
  guard_identity_predicate: id = :caseId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.cases
  mutation_identity_predicate: id = :caseId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - caseId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: triageCorrection
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.corrections
  guard_identity_predicate: id = :correctionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.corrections
  mutation_identity_predicate: id = :correctionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - correctionId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: triageSignal
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.anomaly_signals
  guard_identity_predicate: id = :signalId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: core.anomaly_signals
  mutation_identity_predicate: id = :signalId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - signalId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: unlinkSignalFromCase
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: core.anomaly_signals
  guard_identity_predicate: id = :signalId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: core.anomaly_signals
  mutation_identity_predicate: id = :signalId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - signalId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: updateBudgetLimit
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.budget_limits
  guard_identity_predicate: scope = :scope
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.budget_limits
  mutation_identity_predicate: scope = :scope
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - scope
  context_placeholders: []
  guard_columns:
  - scope
  - version
  mutation_columns:
  - scope
  - version
- operation_id: updateClaim
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.claims
  guard_identity_predicate: id = :claimId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.claims
  mutation_identity_predicate: id = :claimId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - claimId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: updateCorrectionDraft
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.corrections
  guard_identity_predicate: id = :correctionId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.corrections
  mutation_identity_predicate: id = :correctionId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - correctionId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: updateEvidence
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.evidence
  guard_identity_predicate: id = :evidenceId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.evidence
  mutation_identity_predicate: id = :evidenceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - evidenceId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: updateHypothesis
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.hypotheses
  guard_identity_predicate: id = :hypothesisId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.hypotheses
  mutation_identity_predicate: id = :hypothesisId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - hypothesisId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: updateSavedView
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: ops.saved_views
  guard_identity_predicate: id = :savedViewId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.saved_views
  mutation_identity_predicate: id = :savedViewId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - savedViewId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: upgradeProviderModel
  mode: AUTHORIZED_EXECUTION_FENCE
  version_field: expectedVersion
  guard_relation: ops.provider_configs
  guard_identity_predicate: id = :providerId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: ops.provider_configs
  mutation_identity_predicate: id = :providerId
  success_effect: validate and mutate only inside private.ExecuteProviderControl after a current ExecutionAuthorization; direct
    HTTP performs no mutation
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - expectedVersion
  - providerId
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - enabled
  - routing_policy
  - version
  direct_http_effect: NONE
  execution_owner: private.ExecuteProviderControl
- operation_id: validateClaims
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.claims
  guard_identity_predicate: id = :claimId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.claims
  mutation_identity_predicate: id = :claimId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - claimId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
- operation_id: verifyEvidence
  mode: OPTIMISTIC
  version_field: expectedVersion
  guard_relation: editorial.evidence
  guard_identity_predicate: id = :evidenceId
  guard_version_predicate: version = :expectedVersion
  mutation_relation: editorial.evidence
  mutation_identity_predicate: id = :evidenceId
  success_effect: increment version by exactly one; DELETE commands delete exactly one current version
  zero_rows_resolution:
    probe: read identity without version predicate
    missing_error: RESOURCE_NOT_FOUND
    existing_error: VERSION_CONFLICT
  version_mutation: version = version + 1
  request_placeholders:
  - evidenceId
  - expectedVersion
  context_placeholders: []
  guard_columns:
  - id
  - version
  mutation_columns:
  - id
  - version
