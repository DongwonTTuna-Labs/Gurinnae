# PUB-013 — 방법론

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/methodology`  
Surface: `public`  
Access: `anonymous`  
Archetype: `POLICY`  
Test prefix: `pub_013`

## 사용자 목적

구린네의 탐지·조사·공개 방식과 한계를 이해한다.

## 이 화면이 즉시 답해야 하는 질문

- 어떤 규칙을 사용하는가?
- AI와 사람은 각각 무엇을 하는가?
- 오탐을 어떻게 막는가?

## Above-the-fold 순서

1. `overview`
2. `pipeline`
3. `rules`
4. `ai`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `overview` | 방법론 개요 | `LongFormArticle` | 발견과 판정 분리. |
| 2 | `pipeline` | 데이터에서 공개까지 | `StructuredContentSection` | provenance와 human gate. |
| 3 | `rules` | 규칙 목록 | `DataCollection` | active/retired version. |
| 4 | `ai` | AI 역할 | `AccessManagementPanel` | 보조·금지 권한. |
| 5 | `quality` | 평가와 오탐 | `StructuredContentSection` | gold/false-positive. |
| 6 | `changes` | 변경 이력 | `RevisionTimeline` | effective version. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: 12-column grid; sticky table of contents 3 + article 7 + metadata 2
- Medium: 8-column; collapsible table of contents precedes article
- Compact: single long-form column with in-page jump menu
- Container: `public_main`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `open-rule` | 규칙 상세 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `view-coverage` | 데이터 범위 보기 | `NAVIGATION` | `NONE` | `none` | `local-only` |

Primary action: `open-rule`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `listRules` | `public-api` | `GET` | `/v1/rules` | `listRulesQuery` | `RulesPage` |
| `getMethodologyOverview` | `public-api` | `GET` | `/v1/content/methodology` | `getMethodologyOverviewQuery` | `MethodologyOverviewResponse` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `public.methodology.viewed`
- `public.methodology.rule_opened`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- AI 정확도 과장
- 비공개 임계값을 사실 확률로 표시

## Implementation files

- Page: `apps/public-web/src/routes/methodology/+page.svelte`
- Server load/actions: `apps/public-web/src/routes/methodology/+page.server.ts`
- Layout: `apps/public-web/src/routes/+layout.server.ts`
- Error boundary: `apps/public-web/src/routes/+error.svelte`
- View model: `apps/public-web/src/lib/view-models/pub-013.ts`
- E2E: `tests/e2e/screens/pub-013.spec.ts`
- Visual: `tests/visual/pub-013.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
