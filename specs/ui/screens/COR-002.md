# COR-002 — 정정 작업공간

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/internal/corrections/{correctionId}`  
Surface: `internal`  
Access: `oidc`  
Archetype: `WORKSPACE`  
Test prefix: `cor_002`

## 사용자 목적

대상 claim/revision, 요청 근거, 영향, proposed correction, review와 알림을 완결한다.

## 이 화면이 즉시 답해야 하는 질문

- 무엇이 잘못됐고 근거는 무엇인가?
- 어떤 공개 record와 downstream data가 영향을 받는가?
- 정정·철회·기각 중 무엇이 적절한가?

## Above-the-fold 순서

1. `request`
2. `original`
3. `investigation`
4. `proposal`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `request` | 요청 | `EvidenceLedger` | target·issue·evidence. |
| 2 | `original` | 원본 | `StatusAndRevisionHeader` | claim/revision/context. |
| 3 | `investigation` | 검증 | `StructuredContentSection` | source·calculation. |
| 4 | `proposal` | 변경 전후 | `StructuredContentSection` | reason·root cause. |
| 5 | `impact` | 영향 | `StructuredContentSection` | API/download/notification/search. |
| 6 | `review` | 검토·결정 | `DecisionReviewPanel` | editorial/legal/publish. |
| 7 | `receipt` | 요청자 회신 | `StatusAndRevisionHeader` | public-safe status. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: internal shell: 248px nav + 232px task rail + fluid task content + optional 360px context
- Medium: nav collapses; task rail becomes horizontal tabs; context becomes drawer
- Compact: single task at a time; blockers before task body; persistent save state
- Container: `workspace`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `save-proposal` | 정정 초안 저장 | `COMMAND` | `ACTIVE_SESSION` | `publication.correct` | `updateCorrectionDraft` |
| `create-revision` | 정정 개정본 생성 | `DESTRUCTIVE_CONFIRMATION` | `RECENT_SESSION` | `publication.correct` | `createCorrection` |
| `reject-request` | 정정하지 않음 결정 | `COMMAND` | `RECENT_SESSION` | `publication.correct` | `resolveCorrectionRequest` |
| `temporary-limit` | 임시 공개 제한 | `DESTRUCTIVE_CONFIRMATION` | `STEP_UP` | `review.legal` | `placeTemporaryRestriction` |

Primary action: `reject-request`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `getCorrectionWorkspace` | `control-api` | `GET` | `/v1/internal/queries/get-correction-workspace` | `getCorrectionWorkspaceQuery` | `CorrectionWorkspaceResponse` |
| `updateCorrectionDraft` | `control-api` | `POST` | `/v1/internal/commands/update-correction-draft` | `updateCorrectionDraftRequest` | `updateCorrectionDraftReceipt` |
| `createCorrection` | `control-api` | `POST` | `/v1/internal/commands/create-correction` | `createCorrectionRequest` | `createCorrectionReceipt` |
| `resolveCorrectionRequest` | `control-api` | `POST` | `/v1/internal/commands/resolve-correction-request` | `resolveCorrectionRequestRequest` | `resolveCorrectionRequestReceipt` |
| `placeTemporaryRestriction` | `control-api` | `POST` | `/v1/internal/commands/place-temporary-restriction` | `placeTemporaryRestrictionRequest` | `placeTemporaryRestrictionReceipt` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`
- `unauthorized`
- `forbidden`
- `conflict`
- `reauth-required`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `internal.correction.viewed`
- `internal.correction.proposal_saved`
- `internal.correction.resolved`
- `internal.correction.restriction_placed`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- 원본 overwrite
- 요청 자동 승인/기각
- root cause 누락

## Implementation files

- Page: `apps/review-console/src/routes/internal/corrections/[correctionId]/+page.svelte`
- Server load/actions: `apps/review-console/src/routes/internal/corrections/[correctionId]/+page.server.ts`
- Layout: `apps/review-console/src/routes/internal/+layout.server.ts`
- Error boundary: `apps/review-console/src/routes/+error.svelte`
- View model: `apps/review-console/src/lib/view-models/cor-002.ts`
- E2E: `tests/e2e/screens/cor-002.spec.ts`
- Visual: `tests/visual/cor-002.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
