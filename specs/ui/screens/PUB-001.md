# PUB-001 — 홈

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/`  
Surface: `public`  
Access: `anonymous`  
Archetype: `EVIDENCE_LANDING`  
Test prefix: `pub_001`

## 사용자 목적

서비스의 역할을 이해하고 공개 기록 또는 검색으로 진입한다.

## 이 화면이 즉시 답해야 하는 질문

- 구린네는 무엇을 하는가?
- 최근 어떤 기록이 공개·설명·정정됐는가?
- 데이터는 얼마나 최신인가?

## Above-the-fold 순서

1. `mission`
2. `search`
3. `recent`
4. `corrections`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `mission` | 서비스 정의와 한계 | `StructuredContentSection` | 이상 징후와 위법·비리 확정의 구분 원칙. |
| 2 | `search` | 통합 검색 | `UnifiedSearch` | 기관·업체·계약·사건 검색 진입점이다. |
| 3 | `recent` | 최근 공개 기록 | `StatusAndRevisionHeader` | 상태 균형을 반영한 최신 공개 기록. |
| 4 | `corrections` | 최근 설명·정정·철회 | `RevisionAndCorrectionPanel` | 오류·해소 이력을 포함한 정정·철회 기록. |
| 5 | `coverage` | 데이터 범위와 최신성 | `CoverageStatement` | 포함 source와 지연을 요약한다. |
| 6 | `method` | 방법론 | `LongFormArticle` | 탐지 방식과 판정 한계. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: 12-column public grid; status/title 12; known-unknown-response 12; main evidence 8 + sticky context 4
- Medium: 8-column grid; all primary regions span 8; context follows comparison
- Compact: single column preserving status → known → unknown → response → comparison → evidence → history
- Container: `public_main`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `search` | 검색 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `browse-cases` | 사례 보기 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `subscribe` | 업데이트 구독 | `NAVIGATION` | `NONE` | `none` | `local-only` |

Primary action: `search`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `listPublicCases` | `public-api` | `GET` | `/v1/cases` | `listPublicCasesQuery` | `PublicCasesPage` |
| `listCorrections` | `public-api` | `GET` | `/v1/corrections` | `listCorrectionsQuery` | `CorrectionsPage` |
| `listSourceStatus` | `public-api` | `GET` | `/v1/sources/status` | `listSourceStatusQuery` | `SourceStatusPage` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `public.home.viewed`
- `public.search.started`
- `public.home.record_opened`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- 기관·업체 순위
- 가격 배수 top list
- click 기반 trending

## Implementation files

- Page: `apps/public-web/src/routes/+page.svelte`
- Server load/actions: `apps/public-web/src/routes/+page.server.ts`
- Layout: `apps/public-web/src/routes/+layout.server.ts`
- Error boundary: `apps/public-web/src/routes/+error.svelte`
- View model: `apps/public-web/src/lib/view-models/pub-001.ts`
- E2E: `tests/e2e/screens/pub-001.spec.ts`
- Visual: `tests/visual/pub-001.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
