# RSP-002 — 요청 내용

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/respond/overview`  
Surface: `response`  
Access: `response-token`  
Archetype: `GUIDED_FORM`  
Test prefix: `rsp_002`

## 사용자 목적

질문·관련 공개 claim·기한·공개 사용 조건을 이해하고 답변 계획을 세운다.

## 이 화면이 즉시 답해야 하는 질문

- 정확히 어떤 질문에 답해야 하는가?
- 어떤 자료가 참고됐는가?
- 답변은 어떻게 공개될 수 있는가?

## Above-the-fold 순서

1. `questions`
2. `references`
3. `deadline`
4. `public-use`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `questions` | 질문 | `GuidedFormSection` | 중립적 구조화 질문. |
| 2 | `references` | 관련 자료 | `EvidenceLedger` | 공개 claim/evidence만. |
| 3 | `deadline` | 기한·연장 | `StructuredContentSection` | 현재 due. |
| 4 | `public-use` | 공개 범위 | `CoverageStatement` | 동의 선택 설명. |
| 5 | `support` | 지원 | `StructuredContentSection` | 담당자와 안전 채널. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: 12-column; form centered in columns 3–10; summary rail columns 10–12 only on review step
- Medium: 8-column centered
- Compact: single column; sticky save/continue bar does not cover fields
- Container: `response`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `start-answer` | 답변 작성 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `request-extension` | 기한 연장 요청 | `NAVIGATION` | `NONE` | `none` | `local-only` |
| `download-request` | 요청서 저장 | `DOWNLOAD` | `NONE` | `none` | `local-only` |

Primary action: `start-answer`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `getResponseRequest` | `submission-api` | `GET` | `/v1/response-session` | `getResponseRequestQuery` | `ResponseRequestResponse` |
| `downloadResponseRequest` | `submission-api` | `GET` | `/v1/response-session/download` | `downloadResponseRequestQuery` | `BinaryDownload` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `response.request.viewed`
- `response.answer.started`
- `response.extension.started`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- 내부 hypothesis/note 노출
- 무응답=인정 문구

## Implementation files

- Page: `apps/response-portal/src/routes/respond/overview/+page.svelte`
- Server load/actions: `apps/response-portal/src/routes/respond/overview/+page.server.ts`
- Layout: `apps/response-portal/src/routes/+layout.server.ts`
- Error boundary: `apps/response-portal/src/routes/+error.svelte`
- View model: `apps/response-portal/src/lib/view-models/rsp-002.ts`
- E2E: `tests/e2e/screens/rsp-002.spec.ts`
- Visual: `tests/visual/rsp-002.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
