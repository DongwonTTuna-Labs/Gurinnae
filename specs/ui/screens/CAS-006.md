# CAS-006 — 공개 주장 작성

상태: **FINAL / REQUIRED COMPLETE**  
Route: `/internal/cases/{caseId}/claims`  
Surface: `internal`  
Access: `oidc`  
Archetype: `WORKSPACE`  
Test prefix: `cas_006`

## 사용자 목적

공개하려는 정확한 문장을 type·evidence·반대 근거·한계와 함께 작성한다.

## 이 화면이 즉시 답해야 하는 질문

- 이 문장은 사실·계산·해석·당사자 주장 중 무엇인가?
- 충분한 evidence와 반대 근거가 있는가?
- 표현 정책을 지키는가?

## Above-the-fold 순서

1. `summary`
2. `claims`
3. `editor`
4. `lint`

1440×900 wide viewport에서 사용자는 스크롤 전에 화면 목적, 객체 상태, 가장 중요한 답, 주요 행동, freshness 또는 blocker를 확인해야 한다.

## 최종 정보 순서

| 순서 | Section ID | 제목 | 구현 Component | 목적 |
|---:|---|---|---|---|
| 1 | `summary` | claim readiness | `ClaimWorkbench` | invalid/blocking. |
| 2 | `claims` | claim 목록 | `DataCollection` | type·visibility·review. |
| 3 | `editor` | 작성 | `StructuredContentSection` | text·citations·limitation. |
| 4 | `lint` | 언어·정책 검사 | `LongFormArticle` | prohibited wording. |
| 5 | `relationships` | 근거·소명 | `KnownUnknownResponse` | support/contradict. |

이 순서는 wide, medium, compact에서 의미적으로 동일하다. 화면 폭은 배치를 바꾸지만 중요도를 바꾸지 않는다.

## Layout

- Wide: internal shell: 248px nav + 232px task rail + fluid task content + optional 360px context
- Medium: nav collapses; task rail becomes horizontal tabs; context becomes drawer
- Compact: single task at a time; blockers before task body; persistent save state
- Container: `workspace`

## Actions

| Action ID | Label | Interaction | Assurance | Capability | Operation |
|---|---|---|---|---|---|
| `add-claim` | 주장 추가 | `COMMAND` | `ACTIVE_SESSION` | `claims.author` | `addClaim` |
| `save-claim` | 주장 저장 | `COMMAND` | `ACTIVE_SESSION` | `claims.author` | `updateClaim` |
| `validate` | 공개 문구 검사 | `COMMAND` | `ACTIVE_SESSION` | `claims.author` | `validateClaims` |

Primary action: `add-claim`

## Data contracts

| Operation | API | Method | Path | Request Schema | Response Schema |
|---|---|---|---|---|---|
| `listCaseClaims` | `control-api` | `GET` | `/v1/internal/queries/list-case-claims` | `listCaseClaimsQuery` | `CaseClaimsPage` |
| `addClaim` | `control-api` | `POST` | `/v1/internal/commands/add-claim` | `addClaimRequest` | `addClaimReceipt` |
| `updateClaim` | `control-api` | `POST` | `/v1/internal/commands/update-claim` | `updateClaimRequest` | `updateClaimReceipt` |
| `validateClaims` | `control-api` | `POST` | `/v1/internal/commands/validate-claims` | `validateClaimsRequest` | `validateClaimsReceipt` |

모든 Submission operation은 raw bearer token을 canonical URL에 남기지 않는다. Magic token은 BFF가 one-time exchange에만 사용하고 이후에는 scoped submission session으로 전환한다.

## Required states

- `loading`
- `success`
- `empty`
- `partial`
- `stale`
- `error`
- `unauthorized`
- `forbidden`
- `conflict`
- `reauth-required`

상태가 달라도 동일한 정보 위계를 유지한다. Skeleton은 실제 content shape와 일치해야 하며 가짜 성공 값은 표시하지 않는다.

## Responsive contract

- Compact에서 주요 action과 critical unknown/blocker를 숨기지 않는다.
- Primary table은 의미가 보존되는 record list로 재배치한다.
- Context panel은 drawer 또는 inline section으로 이동하며 focus가 복원된다.
- Horizontal scrolling만으로 핵심 작업을 해결하지 않는다.

## Accessibility contract

- H1은 정확히 하나다.
- Section heading level을 건너뛰지 않는다.
- Keyboard만으로 전체 task를 완료할 수 있다.
- 최소 pointer target은 44×44 CSS px다.
- 상태는 색상에만 의존하지 않는다.
- Dialog/drawer 종료 후 원래 trigger로 focus를 되돌린다.
- Chart가 있으면 같은 사실을 담은 표 또는 문장형 대안을 제공한다.
- Save/submit/session exchange 결과를 적절한 live region으로 알린다.

## Analytics allowlist

- `internal.claims.viewed`
- `internal.claim.created`
- `internal.claim.validation_failed`

본문·magic token·scoped session·개인정보·첨부파일명·근거 원문은 analytics payload에 포함하지 않는다.

## 금지

- evidence 없는 factual claim
- AI 자유문 자동 저장
- 부패 확률

## Implementation files

- Page: `apps/review-console/src/routes/internal/cases/[caseId]/claims/+page.svelte`
- Server load/actions: `apps/review-console/src/routes/internal/cases/[caseId]/claims/+page.server.ts`
- Layout: `apps/review-console/src/routes/internal/+layout.server.ts`
- Error boundary: `apps/review-console/src/routes/+error.svelte`
- View model: `apps/review-console/src/lib/view-models/cas-006.ts`
- E2E: `tests/e2e/screens/cas-006.spec.ts`
- Visual: `tests/visual/cas-006.spec.ts`

## Acceptance

- Route와 access mode가 일치한다.
- Section 순서를 바꾸지 않는다.
- 모든 blocking operation을 generated client의 server-only wrapper로 호출한다.
- 모든 필수 상태를 실제 계약으로 구현한다.
- Compact에서 정보와 행동이 소실되지 않는다.
- 권한과 scoped session은 server가 다시 검사한다.
- Screen-specific 금지사항과 global editorial/security policy를 위반하지 않는다.
