# Final Visual Reference

상태: **FINAL DESIGN REFERENCE**

이 정적 reference는 합성 데이터로 최종 디자인 토큰, 정보 위계, layout과 component 성격을 보여준다.

```text
index.html?view=home
index.html?view=case
index.html?view=search
index.html?view=agency
index.html?view=workspace
index.html?view=review
index.html?view=response
```

권위 순서:

1. `FINAL_DESIGN_SYSTEM.md`
2. `design-tokens.yaml`
3. `screen-build-manifest.yaml`
4. `component-catalog.yaml`
5. 이 reference

이 reference의 합성 문구를 production fixture로 복사하지 않는다.
실제 SvelteKit 구현은 generated client, accessibility, 전체 states와 94개 route를 포함해야 한다.
