# CLAUDE.md — 구린내(Gurine) 프로젝트 세션 규칙

## 프로젝트 한 줄

공공 계약·예산·조달 자료의 이상 징후를 증거·소명·정정 이력과 함께 공개하는 증거 우선
플랫폼. 이 리포는 authority pack(specs/)과 그 구현(source tree)이 함께 있다.
완성 판정 = `make verify-final` 전체 통과. 핸드오프는 `CODEX_START_HERE.md` 참고.

## 역할 분담 (fable-sol-loop)

- Claude(이 세션) = 기획·검증·오케스트레이션. 구현은 codex `gpt-5.6-sol`에 위임.
- codex 산출물은 **자기 보고를 믿지 않고** Claude가 직접 게이트 재실행·diff 리뷰로 검증한다.

## UI 리뷰 렌즈 (필수 — 모든 UI diff에 적용)

`specs/ui/interface-restraint.md`가 시각 언어의 BINDING 계약이다 (AGENTS.md §15).
구린내는 잡지가 아니라 **장부(ledger)** — 레퍼런스: https://hosungseo.github.io/korea100/

codex(GPT 계열)는 UI에서 반복적으로 다음 방향으로 드리프트한다. UI 변경을 리뷰할 때
이 목록을 능동적으로 수색할 것:

1. **에디토리얼 드리프트**: 히어로 타이포(>40px), 마케팅·에세이 문단, 페이지 인트로 문단,
   "결론보다 상태와 근거를 먼저 봅니다" 류 자기설명 문구
2. **장식 과잉**: 그라데이션, 그림자 카드 스택, 카드 부풀리기(체크박스 하나를 카드로),
   아이콘·배지 남발, 빈 공간 연출
3. **밀도 파괴**: 대장 화면 첫 화면(1440×900)에 데이터 행 8개 미만이면 실패
4. **보상 해킹**: visual snapshot만 갱신해 게이트 우회, 텍스트만 줄이고 구조 유지,
   클래스명만 바꾸고 레이아웃 동일, 테스트 selector에 맞춘 껍데기 구현
5. **안전 문구 훼손**: 절제한다며 법적 안전 문구(자동 부패 판정 아님 등)를 삭제 —
   삭제 금지, 지정 슬롯 한 줄로 압축이 정답

리뷰 절차:

- UI 변경은 **반드시 실제 렌더를 Playwright로 확인**한다. 코드 diff와 codex의 완료
  보고만으로 시각 품질을 통과시키지 않는다. 전면 변경이면 **94개 라우트를 하나하나 열어
  화면 단위로 직접 판정**한다 (mock-api 하네스: tests/e2e/support, 포트 29100~29103).
  대표 화면 몇 장으로 전체를 추정하지 않는다 — codex는 그럴듯한 대표 화면 뒤에
  나머지를 대충 만드는 성향이 있다.
- 완료 보고 접수 시각과 별개로, codex 턴 **중간에도** 조기 렌더 검사를 한다 — 잘못된
  방향이 94화면에 전파되기 전에 잡는 것이 fix 루프 비용의 수십 분의 일이다.
- **`fullPage` 스크린샷으로 밀도를 판정하지 않는다.** fullPage는 세로로 아무리 길어도
  "다 보인다"는 착시를 준다. 반드시 **실뷰포트(1440×900) above-the-fold 클립 + `document.scrollHeight/900`
  배수**를 측정한다 (interface-restraint §5 G10). korea100 "한 장" 기준: 상세·대장 ≤1.8배,
  폼·워크스페이스 ≤2.2배. 첫 900px에 핵심 정보(대장=데이터 행, 상세=stat/확인·미확인·소명)가 들어와야 한다.
- `specs/ui/interface-restraint.md` §5의 측정 게이트(G1~G8)를 체크리스트로 사용한다.
- 빠른 smell grep: 렌더 문자열에서 "보여줍니다|제공합니다|만나보세요|간편|혁신",
  2문장 이상 `<p>`, 48px급 font-size 토큰.
- **한국어 카피 품질 (사용자 직접 지시)**: 렌더 텍스트에 영어 camelCase 조각("why Material",
  "party Name", "as Of" 류), "확인 항목" 같은 generic 폴백 남발, 어색한 직역체가 없는지
  화면을 직접 보며 검수한다. 필드 라벨은 폐쇄 한국어 맵이어야 하고(미매핑=테스트 실패),
  영어 토큰 폴백 경로는 금지. 동결 카피는 어색해도 임의 수정 금지 — 보고만.

## 작업 관례

- `.fable-sol/`에 기획서·state·last-message를 둔다. codex thread id를 `.fable-sol/state.md`에 기록.
- 커밋/푸시/머지는 사용자가 명시 요청할 때만. PR 직접 머지는 절대 금지.
- specs와 구현이 충돌하면 임의 해석 금지 — `implementation-evidence/spec-conflicts.md`에 기록.
- 완료 선언 전 반드시 검증 evidence(실행 명령 + 결과 원문) 확보. 수행 못 한 검증은 명시.
